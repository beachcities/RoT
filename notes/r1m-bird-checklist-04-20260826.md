# ④ 小型モデル一点実測 — 本走行の報告（2026-08-26）

10:26 に発注のあった④本走行の件、11:05 に完了、所要 **39分**（走行13分、記録の整備を含む）。
**RunPod 課金 $0**、Colab の A100-40GB を **1区間・13分**（うち起動430秒）。

---

## 走行の素性

| | |
| --- | --- |
| モデル | `Qwen/Qwen3.5-2B` |
| 結果 | `run_20260826T014056Z.json`（20試行・**エラー0**） |
| `inputs` 指紋 | **`b451fbf5b1345621`**（開始時に実物で確認、期待値と一致） |
| コミット | **`d87b572` に固定**、`dirty: false` |
| 設計 | `v3_levels` 2課題×全10水準、`REPEATS=1`、試行上限10、seed は `SEED_RULE` |
| 経路 | Colab CLI / A100-SXM4-40GB / vLLM（reasoning-parser なし） |

## 選定理由

`Qwen/Qwen3.5-2B` は 9B と**同一系統・同一世代・同一アーキテクチャ**
（`Qwen3_5ForConditionalGeneration`）で、パラメータが
**2,274,069,824 対 9,653,104,368 の 4.2倍差**。容量だけが違う対になるので、
系統差と交絡しない。Apache-2.0、ゲートなし、取得可能（Hugging Face の API で実物確認）。

## 判定の物差し：境界は出るが、`l8` で崩れる

| 水準 | task_04 | task_06 | 水準計 |
| --- | --- | --- | --- |
| `l0_opaque` | 不正答/10試行/32,520 | 不正答/10試行/38,518 | 71,038 |
| `l1_names` | 不正答/10試行/51,725 | 不正答/10試行/34,370 | 86,095 |
| `l2_units_ref` | 不正答/10試行/30,289 | 不正答/10試行/30,550 | 60,839 |
| `l3_units_doc` | 不正答/10試行/32,440 | 不正答/10試行/32,134 | 64,574 |
| `l4_units_record` | **正答**/1試行/2,626 | 不正答/10試行/32,685 | 35,311 |
| `l5_codes_ref` | 不正答/10試行/35,925 | 不正答/10試行/33,745 | 69,670 |
| **`l6_codes_doc`** | **正答**/2試行/5,408 | **正答**/1試行/2,812 | **8,220** ← **8.5倍** |
| `l7_codes_record` | **正答**/2試行/5,520 | **正答**/1試行/2,784 | 8,304 |
| **`l8_flags_record`** | **不正答**/10試行/30,601 | **不正答**/10試行/32,260 | **62,861** |
| `l9_prose` | **正答**/1試行/2,879 | **正答**/1試行/3,093 | 5,972 |

* **境界の位置**: `l5`→`l6` に現れる。段差 **8.5倍**（69,670 → 8,220）。
* **`l6` 未満の正答**: **1/12**（`l4`/`task_04` の1件のみ）
* **`l6` 以上の正答**: **6/8**。`l8_flags_record` が2課題とも不正答で、
  上側の帯から外れている。
* **ERR**: **0件**。測定 ERR（文脈長超過）も基盤エラーも発生していない。
  二分の適用機会は無かった。
* 生成上限（32,768）への到達も0件。

参考: 9B on の同水準（`20260824T061642Z`）では `l8` は正答している。

## 留保

1. **2B は思考テキストを出さない。** `THINKING=on` を送り、サーバも拒んでいない
   （`dropped` なし）が、**20試行すべてで思考タグが1つも現れず**、`thinking_chars`・
   `thinking_source` はいずれも `null`。実質「思考なし」で動作している。
   **比較の主対は 9B off（`20260824T034646Z`）** で、9B on との対には思考有無の差が入る。
2. **途中経過の退避は区間終端で行った**（水準ごとではない）。1区間で収まる見込みで、
   失われうる最大が区間ぶん＝走行全体と同じであり、水準ごと退避は保護を増やさず
   走行中のセッションへ触る危険だけを足すという判断による。実際に1区間で完走した。
   水準ごと退避の実装は本走行の後の判断とする。
3. `REPEATS=1` なので**反復間のばらつきは評価できない**。5反復の本走行で、
   `l6` 未満は水準どうしが重なり、`l6` 以上とだけ分離することが 9B/Olmo で
   確かめられているが、2B についてはこの1反復から同じことは言えない。

## 経緯

* 前回の中断（`notes/r1m-status-20260826.md`）から**引き継ぎは無く、最初から走った**。
* Colab 経路の起動は **430秒**（4回とも430〜440秒で一致）。作業窓2700秒に対し、
  試行は約6分で終わった。
* `colab new` の失敗に備えて**1回だけの再試行**を入れたが、今回は発火していない。
* 終了時、`colab sessions` は **`No active sessions found on server`**。

## 実行して確かめたことと、推測の区別

**確かめたこと**: 上表の全数値、指紋の一致、コミット固定が効いたこと、思考タグが
0件であること、ERR が0件であること、セッションの取り残しが無いこと。

**推測にとどまること**: なし。`l8` で崩れた理由の解釈は稿側の仕事であり、
ここでは事実のみを置いている。
