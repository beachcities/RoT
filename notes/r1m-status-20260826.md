# R1M 状態確認（2026-08-26 09:03）

BIRD 前チェックリスト④（小型モデル一点実測・Colab 経路版）のやり直し判定のための状態確認。
**実行・課金・Colab 走行は行っていない。** 照会はすべて読み取りのみ。

---

## 1. 自分の引継状態

**CLAUDE.md は存在しない**（リポジトリ直下・`benchmark/`・ユーザ設定のいずれにも無し）。
永続メモリのディレクトリにも自セッション向けの記録は無い。

④の発注内容と経緯は、**会話の文脈としては保持している**（前提0〜見積り6の全項目、
モデル選定の理由、事前確認で出た3つの事実）。ただし**ディスク上の記録としては
残っていない**——選定理由をラン記録に書く段階まで到達しなかったため。
会話が切れれば失われる状態にある。

## 2. ローカル作業ツリー

```
git status --short → 変更なし（クリーン）

git log --oneline -3
  d87b572 Pin the clone on the Colab route too
  822ba6b Record the five-repetition subsection
  ac16db5 Sync the English draft: five repetitions put the step above the spread

未pushコミット → なし（origin/main と一致）
```

**未pushの変更も、コミットし忘れた実装も無い。** ④のために入れた Colab 経路の
コミット固定（`--commit`）は `d87b572` として push 済み。

## 3. 前回④走行の残骸

| 場所 | 状態 |
| --- | --- |
| **Colab セッション** | **`No active sessions found on server`**（stale なローカル記録1件は自動整理済み）。**課金は走っていない** |
| `benchmark/results/partial/` | **空**。④の途中経過は残っていない |
| `benchmark/results/` の 2B 結果 | **`run_20260825T135958Z.json` の1件のみ**（1試行）。本測定の部分結果は無し |
| Drive | 使用していない（この経路は Drive を経由しない） |
| リポジトリ | 2B の結果は未コミット・未登録 |

**本測定は実質ゼロからの再開になる。** 区間1は起動440秒＋作業40秒で切れ、試行が
保存される前に終わった。区間2は `colab new が失敗した` で落ちており（セッション
作成の失敗）、そこで走行全体が終了している。したがって引き継げる部分結果は無い。

残っている `run_20260825T135958Z.json` は**事前確認の1セル**
（`l0_opaque` / `task_06`、`inputs` 指紋 `d429de4c6b8a796f` ＝1課題1水準の部分集合
なので本測定とは別物）。本測定の指紋 `b451fbf5b1345621` を持つ 2B の結果は存在しない。

`benchmark/results/runpod/` 配下にある `laneA`・`laneB_*` 等はすべて**完了済みの
9B/Olmo 本走行の残骸**で、④とは無関係。

## 4. モデル選定の記録

**ディスク上には、選定理由の記録は無い。** 残っているのは事前確認ランの JSON に入る
`models: ["Qwen/Qwen3.5-2B"]` というモデルIDだけ。

会話上で確認した内容は次のとおりで、再走行時にラン記録へ書ける状態にある
（数値は Hugging Face の API で実物確認済み）。

* **`Qwen/Qwen3.5-2B`**、パラメータ **2,274,069,824**
  （9B は 9,653,104,368、**4.2倍差**）
* アーキテクチャは9Bと**同一**（`Qwen3_5ForConditionalGeneration`）、同一系統・同一世代
* Apache-2.0、ゲートなし、取得可能

---

## 判定材料としての留保（前回の事前確認で判明）

1. **2B は思考テキストを出さない。** `THINKING=on` を送り、サーバも拒んでいないのに、
   10試行すべてで思考タグが1つも現れなかった（`thinking_chars` = null、
   `thinking_source` = null）。9B は `closing_tag` 経路で取れている。
   この対は「思考あり9B 対 **思考なし**2B」になり、容量以外の差が1つ入る。
2. **2B は桁違いに軽い。** 最も重い `l0_opaque`/`task_06` が10試行・38,518トークン・
   **9.7秒**（9B は同水準で 83,772トークン）。当初見積り「1〜2時間」は過大で、
   実際は**20〜25分**の規模。
3. **Colab 経路の立ち上げは実測430〜440秒。** 61.5分の上限に対し実効の作業時間は
   約54分。前回、作業窓を480秒に置いたのは**こちらの設定の誤り**で、起動時間を
   差し引くと作業が40秒しか残らなかった。再走行では作業窓を2700秒程度に置くのが妥当。

## 実行して確かめたことと、推測の区別

**確かめたこと**：上記のファイル一覧、git の状態、Colab にセッションが無いこと、
各結果ファイルの中身（モデル・試行数・指紋）、中断ログの末尾。

**推測にとどまること**：区間2の `colab new` が失敗した原因。ログにエラー本文が
出ておらず、A100 の在庫かセッション上限かは切り分けていない。
