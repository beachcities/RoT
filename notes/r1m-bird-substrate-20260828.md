# BIRD Transaction substrate check — 報告（2026-08-28）

08:50 に発注のあった本件、10:05 に結果が出たので報告します。所要 **1時間15分**。
**課金 $0**（GPU 走行なし。**モデルは一度も呼んでいない**）。

**`benchmark/` 配下は変更していない。** 作業は scratchpad。
**対象は既検分の2候補のみ**（新候補の探索・提案はしていない）。
**合否判定・閾値適用は行っていない**——事実計測のみ。

---

## 計測上の固定（取得ファイルの素性）

すべて認証不要の `file-download?statInfId=<id>&fileKind=1` で取得。

| 候補 | statInfId | 表 | bytes | 取得(UTC) | sha256（先頭16） |
| --- | --- | --- | ---: | --- | --- |
| 1 | `000040479792` | 日別調査(令和8年6月1日) 野菜 主要卸売市場計 | 12,686 | 2026-08-27T21:22:15Z | `be335764b7b86411` |
| 1 | `000040479793` | 同 仙台市 | 9,201 | 21:22:16Z | `50d120078aa1a842` |
| 1 | `000040479794` | 同 東京都 | 12,775 | 21:22:16Z | `0e7ac1a3d53cd166` |
| 1 | `000040479795` | 同 横浜市 | 10,886 | 21:22:17Z | `18b809cb1a7d67e1` |
| 1 | `000040479796` | 同 金沢市 | 7,359 | 21:22:17Z | `474105a542d1d803` |
| 1 | `000040479797` | 同 名古屋市 | 10,546 | 21:22:17Z | `53e827d1b2bf36a6` |
| 1 | `000040479798` | 同 京都市 | 10,520 | 21:22:18Z | `f47245f0bf419d32` |
| 1 | `000040479799` | 同 大阪市 | 10,657 | 21:22:18Z | `ed70042fac25e2d0` |
| 1 | `000040479800` | 同 神戸市 | 8,281 | 21:22:19Z | `a99a9f379ae0d100` |
| 2 | `000040479787` | 月報(概数) 令和8年3月分 保管第7表 | 3,106,764 | 21:22:12Z | `f27d4459eb68e7a9` |
| 2 | `000040479777` | 月報(概数) 令和8年3月分 第4表 | 10,823 | 21:22:07Z | `766dceeba30fe8dd` |

完全な sha256 は `scratchpad/estat/sub/manifest.json` に保存。
**候補1は日次更新のため、同じ URL が将来同じ内容を返す保証はない。**

---

# 候補1：青果物卸売市場調査（日別調査）

## 1. 同型データ単位の総数

| | |
| --- | --- |
| 調査全体のファイル数（**機械列挙**） | **130,292 件**（一覧の「130,292 件のデータ」表示。2,606ページ × 50件と整合） |
| うち日別調査（速報）の割合 | **76.5%**（**推定**——22ページを層別標本して算出） |
| **日別調査の表数** | **≈ 99,600 表**（**推定**） |
| 1日あたりの表数 | **30 表**（市場15 × 品目区分2、表名から観測） |
| **日数換算** | **≈ 3,320 日ぶん**（**推定**） |
| 観測された日付の範囲 | 令和8年7月2日（最新）〜 平成24年12月13日（標本内で最古） |

**推定である理由**：一覧は速報（日別 CSV）と確報（年報 Excel）が混在し、
かつページ順が日付順に単調でないため（page 1000 と page 2550 以降は確報のみ）、
全ページを列挙しない限り厳密な件数は出ない。**22ページの標本比率から外挿した。**
全2,606ページの列挙は行っていない。

## 2・4. 実データ側の観測値レコード総数と内訳

**粒度：公表された独立観測セル**（市場 × 日 × 品目 × 産地 の数量・価格）。
**個票（取引明細）ではない。** 元となる取引件数は公表されておらず、
**推定してレコード数に加算していない。**

計測した9表（令和8年6月1日・野菜）の内訳：

| statInfId | 市場 | 全行 | 明細行 | 合計行（派生） |
| --- | --- | ---: | ---: | ---: |
| `…792` | 主要卸売市場計 | 245 | 187 | 58 |
| `…793` | 仙台市 | 146 | 93 | 53 |
| `…794` | 東京都 | 200 | 142 | 58 |
| `…795` | 横浜市 | 172 | 115 | 57 |
| `…796` | 金沢市 | 117 | 66 | 51 |
| `…797` | 名古屋市 | 161 | 106 | 55 |
| `…798` | 京都市 | 167 | 112 | 55 |
| `…799` | 大阪市 | 168 | 111 | 57 |
| `…800` | 神戸市 | 132 | 80 | 52 |
| **計** | | **1,508** | **1,012** | **496** |

* **派生（品目ごとの合計）行は全体の 32.9%。** 産地コードが空欄の行がそれにあたる。
* **明細のみなら 1,012 行**（9表・1日・野菜のみ）。
* **`主要卸売市場計` の表そのものが市場をまたぐ集約**で、他14市場の再掲にあたる。

## 3. 独立軸と水準数

| 軸 | 水準数 | 備考 |
| --- | ---: | --- |
| 時間（日付） | ≈ 3,320（**推定**） | 1日=1表群 |
| 市場 | **15** | 一覧の表名から機械抽出。うち `主要卸売市場計` は集約 |
| 品目区分 | **2** | 野菜／果実 |
| 品目コード | **58**（観測・野菜9表） | |
| 産地コード | **45**（観測・野菜9表） | |

## 5. コードごとの実出現分布（独立単位＝9表を跨ぐ、明細行のみ）

| コード集合 | 種類 | 最小 | 中央 | 最大 |
| --- | ---: | ---: | ---: | ---: |
| 全品目コード | 47 | **10** | 20 | 63 |
| 全産地コード | 45 | **1** | 14 | 119 |

**課題対象コード集合（凍結済み：品目4＋産地2）は発注文に具体的なコード値の指定が
なかったため、こちらでは確定できなかった。** 代わりに**観測度数の上位で代用**した値を
参考として置く（**これは代用であって凍結集合ではない**）：

* 品目4（代用）：`30000`(63), `34200`(41), `31900`(36), `34100`(33)
* 産地2（代用）：`008`(119), `020`(73)

凍結集合のコード値をいただければ、同じ手順でその集合の分布を出す。

## 6. 派生・重複構造（実測）

1. **合計行と明細の関係は「和」になっていない。**
   東京都・品目 `30100`（だいこん）で、明細3件の数量和 **304,731** に対し
   合計行は **352,068**（差 **47,337**）。
   **合計行には明細に落ちない分が含まれる**——単純に足し引きすると合わない。
   **明細のみを数える場合も、合計行を「明細の和」として扱ってはならない。**
2. **同一観測の複数表掲載がある。** `主要卸売市場計` の品目 `30000` は 14,235,648 で、
   東京都＋大阪市だけの和 7,342,365 を含む。**市場計は15市場の集約表**。
3. **概数/確報の派生関係**：日別調査は「速報」、年報は「確報」。同一調査内で
   **提供分類が2系統**ある（一覧の機械抽出で確認）。両者の値の対応は**確かめていない**。

---

# 候補2：人口動態調査 月報（概数）保管第7表

## 1. 同型データ単位の版数

| | |
| --- | --- |
| 人口動態調査のファイル総数（機械列挙） | **57,115 件**（1,143ページ × 50） |
| 提供分類（標本10ページで観測） | 月報（概数）／確定数／速報／正誤情報 |
| 月報（概数）1か月あたりの表数 | **17 表**（第1〜7表＋保管第1〜10表。ID `…774`〜`…790` の連番で観測） |
| **保管第7表** | **1か月につき1版** |
| 観測された調査年月 | 2025年12月〜2026年3月（page 1〜5）、2024〜2025年（page 50） |
| 確定数の年次範囲（標本） | 1986年〜2021年（page 150〜1143 の標本） |

**版数は厳密には列挙していない**（1,143ページ全走査をしていない）。
月報が毎月1版として、**保管第7表の版数は月報の公開月数と同数**になる——
**これは構造からの推定であり、実数は数えていない。**

## 2・4. 実データ側の観測値レコード総数と内訳（`000040479787`・令和8年3月分）

**粒度：公表された集計セル**（地域 × 死因分類 × 性 × 年齢階級 の死亡数）。
**個票ではない。** 元の死亡票の件数は公表されておらず、**加算していない。**

| | 件数 |
| --- | ---: |
| ファイル総行 | 29,824 |
| **コード行（明細）** | **29,607** |
| うち性 `総数`（派生） | 9,869（**33.3%**） |
| うち性 `男`／`女` | 9,869 / 9,869 |
| 地域 `全国`（全地域の集約＝派生） | 417 |
| 地域 `不詳` | 417 |
| 年齢階級の列 | **27 列**（総数列を除く） |

**派生をすべて除いた独立観測セル数**（最下層コード × 男女 × 都道府県）：
**9,964 セル**（最下層コード 106 × 性2 × 都道府県47 の理論値と一致）。

## 3. 独立軸と水準数

| 軸 | 水準数 | 備考 |
| --- | ---: | --- |
| 時間（月） | 月報の公開月数（**未列挙**） | この表は1版 |
| 地域 | **71** | 全国＋都道府県47＋指定都市等20＋不詳 |
| 死因簡単分類 | **139** | 3層の入れ子（下記） |
| 性 | **3** | 総数・男・女（総数は派生） |
| 年齢階級 | **27** | |

## 5. コードごとの実出現分布（地域を跨ぐ）

| | 種類 | 最小 | 中央 | 最大 |
| --- | ---: | ---: | ---: | ---: |
| 全死因コード（出現行数） | 139 | 213 | 213 | 213 |
| 各コードで `総数>0` の地域数 | 139 | **0** | 69 | 71 |

* 出現行数はすべて 213（＝71地域 × 性3）で**完全に均衡**——設計上、全コードが全地域に
  行として存在する。
* **値が全地域で 0 または `-` のコードが 5 種**ある。
* **課題対象コード集合（凍結済み：死因4）は発注文にコード値の指定がなく、確定できなかった。**
  指定をいただければ同じ手順で出す。

## 6. 派生・重複構造（実測）

1. **性の `総数` は男＋女の派生。** 全国 `01000`：総数 2,006 ＝ 男 1,008 ＋ 女 998。**一致。**
2. **死因分類は3層の入れ子。**

   | 層 | 種類 | 例 |
   | --- | ---: | --- |
   | 第1層（下3桁 `000`） | 20 | `01000`, `02000`, … |
   | 第2層（下2桁 `00`） | 57 | `01100`, `01200`, … |
   | 第3層（それ以外） | 62 | `01201`, `01202`, … |

   検算：`01000` (2,006) ＝ 第2層の子6種の和 (2,006) **一致**。
   `01200` (100) ＝ 第3層の子 `01201`+`01202` の和 (100) **一致**。
   **層を跨いで足すと二重計上になる。**
3. **地域は再掲構造。** 全国 `01000` (2,006) ＝ 都道府県47の和 (2,006) **一致**。
   **指定都市20種は都道府県に含まれる再掲**で、別行として重複計上される。
4. **同一観測が複数表に掲載される。** 同じ月の第4表（`000040479777`）の `01000` 当月実数は
   **2,006** で、保管第7表の全国 `01000` と**同値**。

---

## 触れていないもの・読めなかったもの（推測と明示）

* **モデルは一度も呼んでいない。** リーク検査は行っていない。
* **候補1の日別調査表数（≈99,600）と日数（≈3,320）は標本からの推定**。
  全2,606ページの列挙はしていない。
* **候補2の版数（保管第7表の月数）は列挙していない。** 構造からの推定にとどまる。
* **候補1で実測したのは令和8年6月1日・野菜の9表のみ**（15市場のうち9、果実は0表）。
  果実側の分布・行数は**未計測**。
* **候補1の速報と確報の値の対応関係は確かめていない。**
* **候補2で実測したのは令和8年3月分の保管第7表と第4表のみ。** 他の月・確定数は未計測。
* **課題対象コード集合（凍結済み）のコード値が発注文になく、代用で計算した。**
  凍結集合そのものの分布は**出せていない**。
* **個票（取引明細・死亡票）の件数は公表物から得られないため、推定も加算もしていない。**
* **合否判定・閾値適用は行っていない**（発注どおり）。

## 実行して確かめたことと、推測の区別

**確かめたこと**：上表のすべての実測値、取得ファイルの sha256・取得日時・URL、
候補1の合計行と明細の**不一致（差 47,337）**、候補2の性・分類階層・地域の**一致検算**、
死因分類が3層であること、同一観測が第4表と保管第7表の双方に載ること、
候補1の一覧総数 130,292 と候補2の 57,115。

**推測にとどまること**：候補1の日別調査表数と日数（標本外挿）、候補2の版数、
候補1の速報／確報の対応、未取得の表の内容。
