# BIRD Contamination smoke — 手順3で停止（2026-08-28）

**発注時刻：取得不能**（R1M 側で機械取得できないため、推定していない）。
作業開始 **2026-08-27T22:37:15Z**、完了 **2026-08-27T22:40:13Z**、
**所要 2分58秒**（いずれも `date` によるシステム時刻の機械取得。差から算出）。

**課金 $0**（RunPod 不使用、Colab も起動していない）。
**モデルは一度も呼んでいない。**

---

## 結論

**手順3の STOP 条件 `S2` に該当したため、モデルを呼ばずに停止した。**

| 条件 | 判定 | 実測 |
| --- | --- | --- |
| S3（死因×性の2軸交差が独立観測セルとして構成可能） | **通過** | 該当セル 94 件 |
| S5（正答が0でも全件でもない） | **通過** | T1=291, T2=497 / スコープ全件 134,982 |
| 弁別条件（正答と全誤解釈の差が5以上） | **通過** | 最小差 5（下表） |
| S6（派生除外後の正答が独立二経路で一致） | **通過** | 両経路とも T1=291 / T2=497、スコープ 10,434 セルも一致 |
| **S2（課題用スコープの最小形が X_tok=47,104 以内）** | **該当（停止）** | **最小形 141,667 トークン**（超過 94,563） |

発注どおり、**設計変更の提案はしない。**

---

## 手順1：snapshot 固定

| ファイル | URL | bytes | sha256 |
| --- | --- | ---: | --- |
| `body.csv` | `…/stat-search/file-download?statInfId=000040479787&fileKind=1` | 3,106,764 | `f27d4459eb68e7a99eb4aecce4a6541aae694236d936db5a65154946978e6cf0` |
| `codelist.html` | `…/dbview?sid=0003411702` | 560,201 | `3f427a09e51d4a02961d6743f02dd097034680aec528c6d54a33e5372ee0bf40` |

**取得日時：2026-08-27T22:37:15Z 〜 22:37:23Z**（UTC、機械取得）。
以後の全工程はこの snapshot のみを使った。
`body.csv` の sha256 は 8月27日 21:22Z 取得分と**同一**——この間に更新されていない。

## 手順2：課題コードの実体化

### 全コード集合

**コード表の異なりコードは 140 件**であって、発注文の「150」とは一致しない。
内訳を機械確認した：

* `(コード, 名称)` の異なり対は **150**。
* **異なりコードは 140**。差の 10 件は、**同一コードに旧称が併記**されているもの
  （例 `02100`：`悪性新生物＜腫瘍＞` と `(2016年以前)悪性新生物`）。
* コード表にあって本体に現れないコードは **1 件**（`00000` 総数）。

**150 は対の数、140 がコードの数。** 事実として記録する（判定は行わない）。

### 階層と非縮退

| | 件数 |
| --- | ---: |
| 本体に現れるコード | 139 |
| 第1層（下3桁 `000`） | 20 |
| 第2層（下2桁 `00`） | 57 |
| 第3層（それ以外） | 62 |
| **最下層** | **111** |
| **非縮退**（全国・性総数が `-` でも 0 でもない最下層） | **106** |

### 課題対象コード集合（非縮退をコード値昇順、先頭4件）

| | コード | 名称 | 全国・性総数 |
| --- | --- | --- | ---: |
| #1 | `01100` | 腸管感染症 | 205 |
| #2 | `01201` | 呼吸器結核 | 86 |
| #3 | `01202` | その他の結核 | 14 |
| #4 | `01300` | 敗血症 | 960 |
| 次 | `01401` | Ｂ型ウイルス性肝炎 | 23 |

`01401` は誤解釈 (b) 用の「昇順で次の非縮退コード」。

### 課題テンプレートの実体化（参照コードのみ）

* **T1** ＝ コード `01100`・`01201` に該当する死亡数の合計（全国・性総数）
* **T2** ＝ コード `01202`・`01300` に該当する死亡数のうち性＝男の合計（全国）

**文面は F1 付表とされており、発注文に含まれていない。**
手順3は参照コードのみで足りるため停止判断に影響しないが、
**プロンプト全文は入手していない**（読めなかったものとして明示）。

## 手順3：STOP 条件の機械確認

### 課題用スコープ（S6 の除外規則）

派生・再掲を機械規則で一意に除外した：

1. 地域は**都道府県47のみ**（`全国` は集約、指定都市20は都道府県内の再掲、`不詳` を除く）
2. 性は**男・女のみ**（`総数` は男＋女の派生）
3. コードは**最下層111のみ**（上位層は下位層の集約）

→ **10,434 セル**（47 × 2 × 111）。

### 正答と隣接誤解釈

| | 値 | 正答との差 |
| --- | ---: | ---: |
| **T1（正答）** | **291** | — |
| (a) `01100` のみ | 205 | 86 |
| (a) `01201` のみ | 86 | 205 |
| (b) ＋`01401` | 314 | 23 |
| **T2（正答）** | **497** | — |
| (a) `01202` のみ | 5 | 492 |
| (a) `01300` のみ | 492 | **5** |
| (b) ＋`01401` | 509 | 12 |
| (c) 性＝総数 | 974 | 477 |

**最小差は 5**（T2 の「`01300` のみ」）——弁別条件（5以上）を**ぎりぎり通過**。

### S6：独立二経路

| | 経路A（行を手分割） | 経路B（`csv` モジュール） | |
| --- | ---: | ---: | --- |
| スコープ件数 | 10,434 | 10,434 | 一致 |
| T1 | 291 | 291 | 一致 |
| T2 | 497 | 497 | 一致 |

### S2：直列化トークン（該当した条件）

課題用スコープ 10,434 セルを直列化したときのトークン数
（トークナイザは `Qwen/Qwen3.5-9B` の `tokenizer.json`、本測定と同一）：

| 形 | トークン | X_tok=47,104 に対し |
| --- | ---: | --- |
| A 整形・名称つき | 534,756 | 超過（×11.4） |
| B 圧縮・名称つき | 311,620 | 超過（×6.6） |
| C 圧縮・列削減（配列） | **141,667** | **超過（×3.0）** |

**最小形でも 141,667 トークンで、上限を 94,563 超える。**

**参考として記録**（判定には使わない）：先の検分（`notes/r1m-bird-estat-feasibility-20260828.md`）
で「収まる」と報告した 10,114 トークンは、**全国スライス417行**を1文書としたときの値で、
今回の**派生除外後の全スコープ 10,434 セル**とは対象が違う。
今回の S2 は発注の「課題用スコープの直列化」に従って全スコープを測っている。

## 手順4以降

**実行していない。** S2 に該当したため、モデル呼び出し・Colab 起動を行っていない。

## 成果物（push 済み）

`notes/bird-smoke/` に次を置いた。

| ファイル | 内容 |
| --- | --- |
| `snapshot.json` | 2ファイルの URL・bytes・sha256・取得日時 |
| `codes.json` | コード表140件、最下層111、非縮退106、課題対象4件、次1件 |
| `stop_conditions.json` | 正答・誤解釈・スコープ定義・S3/S5/弁別/S6/S2 の判定とトークン実測 |
| `stop_check.py` | 手順1〜3を再現する検査スクリプト（**モデルを呼ばない**） |
| `stop_check_output.txt` | 上記スクリプトの判定出力 |

`stop_check.py` を単体で回して**同じ数値が再現する**ことを確認済み。

## 触れていないもの・読めなかったもの（推測と明示）

* **モデルは一度も呼んでいない。** 既知率・棄権率・思考テキストは**測っていない**。
* **プロンプト全文（F1 付表）は発注文に含まれておらず、入手していない。**
  手順4に進んでいないため凍結文面の push もしていない。
* **seed のスモーク専用帯は確定していない**（手順4に属するため）。
* **候補1は扱っていない**（発注どおり）。
* **合否適用は行っていない**（発注どおり。chat の凍結規則による）。
* **コード表140 と発注文の150 の差の原因**は旧称併記であることまで機械確認したが、
  **どちらを「全コード集合」とすべきかの判断はしていない**。
* Colab セッションは**一度も起動していない**ため、取り残しの確認対象がない。

## 実行して確かめたことと、推測の区別

**確かめたこと**：snapshot の sha256 と取得日時、コード表の異なりコード140・対150・
旧称併記10件、階層（20/57/62、最下層111）、非縮退106、課題対象4コードとその全国総数、
スコープ 10,434 セル、T1=291・T2=497、全誤解釈の値と差（最小5）、
二経路の一致、3形のトークン数（最小 141,667）、保存スクリプトでの再現。

**推測にとどまること**：なし（本報告の数値はすべて snapshot からの機械計算）。
ただし上記「触れていないもの」は測っていないので、そこに踏み込んだ記述はしていない。
