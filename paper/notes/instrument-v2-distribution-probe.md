# 計器v2「分布プローブ」仕様——v2.0凍結版

2026-08-24 chat起草・山田レビュー（二次元格子まで合意）。2026-08-26、スモーク（run 20260826T030829Z）と外部レビューを受けて統計的定義を確定し、**本版をv2.0として凍結する**。凍結の対象は本文書の統計的定義（第3〜7節）。以後、本測定完了まで定義の変更を禁じ、バグ修正は申告制とする。スモークは計器開発データ、パイロットは標本数設計データ、本測定は検証データとして分離する。

**凍結後の訂正・追補（2026-08-26、申告）**：(1) 第3節の変種数の式を C(4,d) から **C(2,t)·C(4,d)** へ訂正——実装 `build_v4.py` の実際の変種定義（的の選び方×外れの選び方の直積）との整合訂正。統計的定義（H・γ・断面・lapse系列）は不変。(2) 第3節にseedの**入れ子master集合**方式を追補——セル横断でd曲線を引く際のMonte Carlo変動を抑えるpaired構造のため。

## 1. 目的

現行計器（v3_levels＋再試行ループ）の弱点二つ——段差の高さがMAX_ATTEMPTSの設定値で決まること、成果が正答の二値しかないこと——を、「壊れる」の定義変更で外す。壊れるとは一試行の結末ではなく、**同条件で引いた出力分布が正解に集中しなくなること**。

## 2. 測定の骨格

再試行ループを廃し、一発勝負（試行上限1、再試行プロンプトなし）を同条件でn標本引く。seed群は事前登録し記録（パイロット以降はスモークと重ならない新規帯）。v3系列とは実装非互換の別スイート（v4_distribution）。v3の数値との直接比較はしない。

前提：needs欄修正（1f628bb）後のスイートを基礎とする。inputs指紋は測定側の台帳に記録。

## 3. 格子（二次元）と標本設計

- 格子は **的の記載数 t ∈ {0, 1, 2} × 外れの記載数 d ∈ {0, 1, 2, 3, 4}** の15セル。単一軸に潰すと直接情報（的を書く）と間接情報（外れを書いて残候補を絞る）の二経路が交絡するため。
- 変種は**的の選び方×外れの選び方の直積**で、変種数 **V = C(2,t)·C(4,d)**（t=0行：1,4,6,4,1／t=1行：2,8,12,8,2／t=2行：1,4,6,4,1）。
- 外れ部分集合は標本間で**振る**（スモーク実測：固定は8/10が同一誤答へ集中し、特定変種の癖を測ってしまう）。
- **seedは入れ子master集合**：走行ごとにmaster集合 S1…S12 を事前登録し、各セルは必要数を**先頭から**取る（12要るセルはS1〜S12、6ならS1〜S6、3ならS1〜S3、2ならS1〜S2）。全セルが少なくともS1・S2を共有し、d方向・t方向の比較がpairedに近い構造を持つ。
- 振り方は**完全交差・均衡設計**：**同一セル内では同じseed集合を全変種に交差**させる（変種ごとに別seedを振ると変種効果とseed効果が再び交絡する）。各変種に最低2seedを確保し、variant×seedを二段で記録して変種内／変種間／全体の揺れを分解する。均衡が取れないnは採らない。パイロットの最小設計：t=0行とt=2行は各セルn=12、t=1行はn=12,16,24,16,12——計200標本。

## 4. ヌルモデル（残存仮説数Hと偶然水準γ）

各セルの**残存仮説数 H = C(6−t−d, 2−t)**。無情報の当て推量が残候補から一様に選ぶときの正答率は γ = 1/H。t=0断面でH=15,10,6,3,1、t=1断面でH=5,4,3,2,1、t=2断面は全dでH=1（γ=1）。Hは書かれた的・外れの「どれを」選んだかに依らず「いくつ」書いたかで決まるため、変種間で共通。

## 5. 定式化（v2.0で確定）

- **横軸は x = log₂H（残存不確実性、bit）**。dそのものは横軸にしない——外れ1個の追加は等量の情報増加ではなく、Hの縮み方は組合せで決まるため。
- **t=0とt=1は独立の断面**として扱い、各断面で正答率曲線から**膝m・幅w**を推定する。単一の「情報量」軸への一本化（旧案 ψ=γ+(1−γ−λ)·F）は撤回する——二次元にした設計思想と衝突し、かつスモークで観測されたγ未満の正答率を表現できない。当てはめ族は**下限をγに固定しない形**とし、具体形はパイロットの分布から選ぶ（選択はパイロットデータで行い、本測定では変更しない）。
- **t=2系列はm/w推定の対象外**とし、十分情報条件におけるlapse（取りこぼし率λ）の測定に充てる。
- **H=1の端点の突き合わせ**：t=2（直接記載）に加え、t=0,d=4とt=1,d=4もH=1だが、こちらは消去法による確定。両者の失敗率を「直接記載のλ」「消去確定のλ」として別々に記録する。ただしこれは**記述的比較に留める**——H=1でも文書の内容・長さが異なるため、経路の因果差としては読まない（言えるのは「経路に関連した差」まで）。

## 6. 多義性の系列（情報量軸とは別系列・v2.0の凍結対象外＝未着手）

三水準を並置する——(a) 潰せる多義性、(b) 単位の未決、(c) 除外基準の未決。予想は(a)が正常型・(b)(c)が異常型（仮説であり結果で棄却され得る）。着手時に本仕様へ追補し、追補後に改めて凍結する。

## 7. 秩序変数と壊れ方の指紋・棄権

- **abstention（棄権）の定義**：「mapping不存在等を理由に、回答不能・該当なしと判断したことを明示した回答」。数値0そのものの機械的判定ではない——0が正当な数値解になり得る課題への拡張に備え、理由の明示を要件とする（スモークの4件はいずれもこの型）。
- **二部モデル**：棄権率を独立指標として報告し、山の数・最大山占有率・正解山占有率・回答エントロピーは**非棄権の数値回答**で計算する。棄権込みの値は感度分析として併記。総トークン（分母）は従来どおり全標本。
- 山は数直線上の密度で推定（想定山数は答え合わせ側にのみ置く。隙間ベースの素朴な実装から始め、閾値依存は留保として明記）。
- 壊れ方の指紋は三型を事前登録——**散る**（欠落型：山多数）、**割れる**（未決型：二山が二つの読みの正解値に各々一致）、**ずれる**（全標本が誤った読みに集中）。指紋と原因の対応自体が検証対象であり、外れたら外れたと記録する。

## 8. 段取り（同期点はコード完成でなく定義の凍結）

① 素案確定（済）→ ② 実装（済、8b082ff）→ ③ スモーク（済、検収合格）→ **④ 本版で統計的定義を凍結（済）** → ⑤ 実装を本版へ追随（完全交差・均衡設計・入れ子seed・棄権二部・V/H/γ表・log₂H軸の集計）し、**仕様条項→実装箇所の対応表（preflight）をchatが照合**してからタグ `instrument-v2.0` → ⑥ 新seed帯でパイロット（全15セル・200標本）→ **⑦ パイロット後・本測定前の第二凍結点：fit関数／m・wの計算法／本測定n／除外規則を文章として凍結** → ⑧ 本測定（計器変更禁止）。長時間実行にはチェックポイントと再開を最初から組み込む。

## 9. 本稿（試論）との関係

この計器は稿の第5節の測り方を置き換える候補であり、稿の主張そのものではない。結果が出るまで稿には反映しない。関連する未決：閾値問題（#47）、削れるトークンと本質的に要るトークンの分離（#93）、多義性軸の未着手。
